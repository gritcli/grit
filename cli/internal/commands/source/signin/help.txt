The "source sign-in" command signs in to a repository source, allowing access to
private repositories.

The specific authentication mechanism used depends on which driver the source
uses. For example, you may be prompted for credentials on the terminal, or via a
web page.
